# API Configuration
# For development, use localhost URLs
# For production, replace with your actual backend URLs
VITE_API_URL=http://localhost:3000/api
VITE_WS_URL=ws://localhost:3000

# App Configuration
VITE_APP_NAME=TaskMaster
VITE_APP_VERSION=1.0.0

# IMPORTANT: 
# - Copy this file to .env (it's gitignored)
# - Replace the URLs above with your own backend URLs for production
# - Never commit .env files with actual production credentials
