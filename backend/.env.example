# Database Configuration
# ⚠️ IMPORTANT: Replace with your own database credentials
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=YOUR_DATABASE_PASSWORD_HERE
DATABASE_NAME=taskmaster

# JWT Configuration
# ⚠️ CRITICAL: Replace these with strong, random secrets in production!
# The application will NOT start in production with default values
# Generate secrets using: node scripts/generate-secrets.js
JWT_SECRET=your-super-secret-jwt-key-change-this-minimum-32-characters
JWT_EXPIRATION=7d
JWT_REFRESH_SECRET=your-refresh-secret-minimum-32-characters
JWT_REFRESH_EXPIRATION=30d

# Server Configuration
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:5173

# Redis Configuration (Optional - falls back to in-memory cache if not configured)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

# SMTP Configuration (for email sending - password reset functionality)
# ⚠️ IMPORTANT: Replace with your own email credentials
# For Gmail, you need to generate an App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# Admin User Configuration (for seeding admin user)
# Set these if you want to seed an admin user: npm run seed:admin
# ADMIN_EMAIL=your-admin@email.com
# ADMIN_PASSWORD=your-secure-admin-password
# ADMIN_FIRST_NAME=Admin
# ADMIN_LAST_NAME=User

# IMPORTANT NOTES:
# - Copy this file to .env (it's gitignored)
# - Replace all placeholder values with your own credentials
# - Never commit .env files with actual production credentials
# - For production, use strong, randomly generated secrets

# User Seeding Configuration (Optional - for seed:users command)
# Admin User
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_FIRST_NAME=Admin
ADMIN_LAST_NAME=User

# Regular User
USER_EMAIL=
USER_PASSWORD=
USER_FIRST_NAME=John
USER_LAST_NAME=Doe
